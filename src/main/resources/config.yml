# CDKer 插件主配置文件
# 此文件用于配置插件的全局行为，例如语言和消息前缀。
# 它是插件正常运行所必需的，请勿随意删除。

# 使用的语言。插件将加载 'lang/lang_<language>.yml' 文件。
# 目前支持 'cn' (中文) 和 'en' (英文)。
language: "cn"

# 插件发送到聊天框的消息前缀。
# 支持 Minecraft 颜色代码，例如 &b (青色), &7 (灰色), &f (白色)。
prefix: "&bCDKer &7> &f"